import React, {Component} from "react";
import "./"

export const Button = props => (
            <div>
                <button>
                    {this.type}
                </button>                     
            </div>
);

